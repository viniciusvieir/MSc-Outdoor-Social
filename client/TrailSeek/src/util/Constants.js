const SUCCESS = 'Success',
  LOADING = 'Loading',
  FAILED = 'Failed',
  IDLE = 'Idle'

const POINTS = {
  marginHorizontal: 20,
}

export default {
  SUCCESS,
  LOADING,
  FAILED,
  IDLE,
  POINTS,
}
